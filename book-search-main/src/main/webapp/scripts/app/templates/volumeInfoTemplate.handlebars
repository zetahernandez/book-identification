<div id="main--{{unbound volume.id}}" class="row-fluid">
	<div class="span2">
		{{#if volume.volumeInfo.imageLinks.thumbnail}} 		
				<img border="0" {{bindAttr src="volume.volumeInfo.imageLinks.thumbnail"}} {{action "openVolume"  volume}} {{ bindAttr title="volume.volumeInfo.title"}} {{ bindAttr alt="unbound volume.volumeInfo.title"}}>
		{{/if}}
	</div>
	<div class="span10">
		<div class="volume-info-title">
			<h3>
				<a href="" {{action "openVolume" volume}}>{{volume.volumeInfo.title}}</a>
			</h3>
		</div>
		<div class="volume-info-content">
			<div>
				<span class="volume-info-author">
					<span style="color: #676767">
						<a href="#">
						   <span dir="ltr">{{unbound volume.volumeInfo.authors}}</span>
						</a> 
						- {{volume.volumeInfo.formattedPublishedDate}} -
					</span>
				</span> 
				{{#if volume.volumeInfo.description}}
				<div>
					<div class="more-less">
						<div dir="ltr" class="snippet sa more-block">
							<p>{{volume.volumeInfo.unescapedDescription}}</p>
						</div>
					</div>
				</div>
				{{/if}}
			</div>
		</div>
	</div>
</div>