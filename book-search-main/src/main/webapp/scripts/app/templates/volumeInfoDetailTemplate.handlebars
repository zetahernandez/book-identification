<div id="main--{{unbound volumeInfo.id}}" class="volume-info">
	{{#if volumeInfo.imageLinks.maxSizeImage}}
	<div class="volume-image">
			<a dir="ltr">
			<img border="0"
				class="coverthumb hover-card-attach-point"
				src={{unbound volumeInfo.imageLinks.maxSizeImage}}
				title={{unbound volumeInfo.title}}
			</a>
	</div>
	{{/if}}
	<div class="volume-info-data">
		<div class="volume-info-title">
			<h2>
				<a href="#"><span
					dir="ltr" class="gb-volume-title">{{unbound volumeInfo.title}}</span>&lrm;</a>
			</h2>
		</div>
		<div class="volume-info-content">
			<div>
				<span class="volume-info-author">
					<span style="color: #676767">
						<a href="#">
						   <span dir="ltr">{{unbound volumeInfo.authors}}</span>
						</a> 
						- {{volumeInfo.publishedDate}} -
					</span>
				</span> 
				{{#if volumeInfo.description}}
				<div>
					<div dir="ltr" class="snippet sa more-block">
							<p>{{volumeInfo.unescapedDescription}}</p>
					</div>
				</div>
				{{/if}}
				{{#if volumeInfo.pages}}
				<div dir="ltr">
					Pages: {{volumeInfo.pages}}
				</div>
				{{/if}}
				{{#if volumeInfo.publisher}}
				<div dir="ltr">
					Publisher: {{volumeInfo.publisher}}</div>
				{{/if}}
				{{#if volumeInfo.industryIdentifiers}}
					{{#each isbn in volumeInfo.industryIdentifiers}}
					<div dir="ltr" class="snippet sa">
						{{isbn.type}}: {{isbn.identifier}}
					</div>
					{{/each}}
				{{/if}}
				{{#if path}}
					<div dir="ltr" class="open-book">
						<a href="#" {{action openPdf volumeInfo target="controller" }}>Open Book</a>
					</div>
				{{/if}}
				<div dir="ltr" class="back">
					<a href="#" {{action back target="controller" }}>Back</a>
				</div>
			</div>
		</div>
	</div>
</div>